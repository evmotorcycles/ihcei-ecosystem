# LISM analysis + tau_v monitor. Version floors known to work with the
# analysis scripts. Logistic convergence under separation is version-sensitive
# (bears on referee point M5), so record what you actually ran with.
numpy>=1.24
pandas>=1.5
scipy>=1.10
statsmodels>=0.14
scikit-learn>=1.2

# Verified reproducing environment (this deposit):
#   numpy 2.4.6  pandas 3.0.3  scipy 1.17.1  statsmodels 0.14.6  scikit-learn 1.9.0
# tau_v_monitor core needs only the Python standard library; pytest for tests.
